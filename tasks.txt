- configuration file with datasource information
- from form-search get the information
  pass the inform to soap Handler(i.e. paramaterizing the form information into the soap request)
  soap post these information to datahandler
  dataHandler insert these information into the database
- let classes perform only their specific functions
- create an abstract class for connection and create MSqlConnection class that inherite from connection
- read about MVC => keep view with only tasks. model, view, and controller should be clear
- use angularjs for dynamic divs display
- create a classes folder in which we put all our classes(e.g. classe ya user, class transaction, class ya booking, )
- later have a look at bootstrap countries and cities


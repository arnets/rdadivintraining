- *(2)configuration file with datasource information(database connection)
- (7)from form-search get the information
  pass the inform to soap Handler(i.e. paramaterizing the form information into the soap request)
  soap post these information to datahandler
  dataHandler insert these information into the database
- (5)let classes perform only their specific functions
- ????(3)create an abstract class for connection and create MSqlConnection class that inherite from connection
- *(1)read about MVC => keep view with only tasks. model, view, and controller should be clear
- (6)use angularjs for dynamic divs display
- use ajax in controller
- (4)create a classes folder in which we put all our classes(e.g. classe ya user, class transaction, class ya booking, )
- (8)later have a look at bootstrap countries and cities

